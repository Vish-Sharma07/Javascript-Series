const score = 400
console.log(score); // undefined

const balance = new Number(1234);
console.log(balance); // Specifically specifies that it is a Number

console.log(balance.toString());
console.log(typeof(balance));

console.log(balance.toFixed(1)); // gives a fixed value after decimal i.e. 1 in this case

const otherNumber = 100000
console.log(Math.round(4.6));